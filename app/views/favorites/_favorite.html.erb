<% if post.favorited_by?(current_user) %>
  <%= link_to "", post_favorites_path(post.id), method: :delete, remote: true, class: "fas fa-bookmark fa-2x" %>
<% else %>
  <%= link_to "", post_favorites_path(post.id), method: :post, remote: true, class: "far fa-bookmark fa-2x" %>
<% end %>
